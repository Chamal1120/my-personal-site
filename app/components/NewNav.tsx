/*
 * returns a navbar component
 * uses tailwind for styling
 * uses motion one library for animatiosn
 * uses a list for populating the navitems
 * should be responsive for both desktop and mobile
 * use new supported css for better responsiveness with texts.
 */
export default function NewNav() {}
